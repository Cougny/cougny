# apps/web — Next.js web app environment.
#   cp .env.example .env
# Only NEXT_PUBLIC_* vars are exposed to the browser, and they are inlined at
# build time — rebuild after changing them.

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SIGNALING_URL=ws://localhost:4001
